package buildFeatures

feature('logstashEncoder', 'Logback Logstash Encoder') {
    implementation('net.logstash.logback:logstash-logback-encoder:%LOGSTASH_LOGBACK_VERSION', 'logbackLogstashVersion')
}
